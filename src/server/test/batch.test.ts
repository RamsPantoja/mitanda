import { describe, expect, it } from 'vitest';

describe("fizzbuzz", (): void => {
    it("Should be a function", (): void => {
        // expect(typeof services.batchService.create).toBe("function");
    });
});